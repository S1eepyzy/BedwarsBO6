# BedWars 极速攻防BO6模式 · 最终完整PLAN \+ DEBUG调试方案

## 一、模式基础定位（最终定稿）

- **模式名称**：极速轮换攻防 BO6 BedWars

- **可用档位（严格满编开局）**：1v1\(2人\) / 2v2\(4人\) / 3v3\(6人\) （无4v4）

- **开局机制**：满人自动计分板倒计时，人数掉落立刻重置倒计时

- **赛制**：BO6 先拿4小局胜利夺冠

- **攻防机制**：每小局结束**强制互换攻防**（A攻→A守→A攻循环）

- **整体节奏**：全面快于 Hypixel 原版，压缩发育、强化打架、极速轮转

## 二、全局核心规则（100%定制闭环规则）

### 1\. 复活系统（每小局独立）

- 每名玩家 **每小局固定2次复活机会**

- 复活等待时间：**5秒（比Hypixel更快）**

- 2次复活用完 → 本局直接淘汰成旁观者，无法再次参战

- 每新一小局开局，全员复活次数强制重置为2次，独立结算不跨局

### 2\. 资源系统（极速打架核心）

- 基地生成器：**只刷铁、金，完全不刷钻石**，全局无原生钻石刷新点位

- 钻石唯一来源：**击杀敌方玩家掉落**，无其他获取途径

- 资源刷新速度：**比Hypixel原版提升30%**，大幅压缩苟家发育时间

- 彻底杜绝蹲家发育玩法，强制玩家主动交战争夺高阶资源

### 3\. 装备 \& 升级保存规则

- **团队全局升级（速度、防御、再生、急迫等）：整局BO6永久保存，跨所有小局不重置、不丢失**

- **铁、金子购买的装备、道具：死亡瞬间全部清空销毁**

- 钻石购买的装备、道具同样死亡清空，仅团队全局buff永久留存

- 每小局结束结算后，统一清空所有玩家背包、盔甲、手持物品，仅保留团队升级数据

### 4\. BO6攻防轮换规则（核心独创）

固定轮换顺序（仅第一局由选边决定，后续小局全自动轮换，不受对局胜负影响）1

局1：选边方进攻 / 对手防守

局2：全员攻防身份互换

局3：全员攻防身份互换

局4：全员攻防身份互换

局5：全员攻防身份互换

局6：全员攻防身份互换

**无论单局胜负，每局结束强制换边**，打满6局可保证双方各3次进攻、3次防守，绝对公平

胜利条件：任意队伍率先累计**4小局胜利分**，立刻终止整场BO6赛事，提前夺冠

### 5\. 选边机制

- 1v1模式：赛前硬币随机选边，确定首局攻防身份

- 2v2 / 3v3模式：赛前60秒空手拼刀选边，禁止方块、道具、技能，总击杀数更高队伍获得首局攻防选择权；人头持平则随机选边

### 6\. 大厅满人倒计时机制

- 人数未满档位标准：计分板常驻显示「等待补齐人数」，无倒计时、无开局触发

- 人数满编达标（2/4/6人）：立即触发**10秒全局计分板倒计时**，所有玩家可见

- 倒计时存续期间，任意玩家退出房间：**倒计时立刻终止、任务销毁、状态重置**，回归等待补人状态

- 倒计时正常走完：直接进入选边阶段，开启正式赛事流程

- 禁止人数不达标强行开局，无手动开局按钮，全程自动触发

## 三、极速节奏配置（全面快于Hypixel原版参数）

所有参数为固定可落地配置，直接覆盖原版Hypixel BedWars节奏，主打快节奏、高对抗、无拖沓

- **资源刷新速率**：铁、金生成速度较Hypixel原版提升30%，快速成型基础装备，减少空窗等待时间

- **复活时长**：固定5秒复活，短于Hypixel原版，战斗衔接零断层，持续拉扯对抗

- **资源机制限制**：基地无钻石刷新，彻底杜绝长线苟家发育、蹲点成型玩法

- **战局轮转**：单局必换攻防，避免单一攻防姿态拖沓对局，高频切换战术博弈

- **装备重置机制**：死亡清空低成本装备，杜绝滚雪球碾压，每波交战均为全新博弈

- **升级积累机制**：全局永久团队升级，赛事越往后节奏越紧凑，对抗强度逐步提升

## 四、完整DEBUG调试体系（分步自测\+指令\+排错日志）

全程分步调试，按阶段依次测试，每阶段测试通过后再进入下一阶段，精准定位BUG，支持逐行逻辑校验，适配插件开发调试

### 阶段1：大厅人数\&倒计时逻辑调试（基础功能）

**调试目标**：验证档位人数校验、满编倒计时、退人重置逻辑正常运行

**调试指令/操作**：切换1v1/2v2/3v3档位，增减房间玩家人数

- 测试1：1v1档位仅进入1人 → 计分板显示等待补人，无倒计时，开局锁定

- 测试2：1v1补满2人 → 瞬间触发10秒计分板倒计时，数值实时刷新

- 测试3：倒计时进行中退出1名玩家 → 倒计时立即销毁，状态重置，恢复等待界面

- 测试4：2v2（4人）、3v3（6人）重复上述测试，确认仅满编触发倒计时，缺人即重置

**BUG判定标准**：未满编出现倒计时、退人不重置、满编无倒计时，均为功能异常

### 阶段2：选边\&攻防轮换逻辑调试（核心赛制）

**调试目标**：验证首局选边生效、每局强制互换攻防、轮换顺序无错乱

**调试指令/操作**：完成选边，连续打完多轮小局，核对每局攻防身份

- 测试1：1v1随机选边、2v2/3v3拼刀选边，首局攻防身份匹配选择结果

- 测试2：第1小局结束，两队攻防身份自动互换，无手动干预、无错乱

- 测试3：连续打完6小局，验证轮换顺序严格交替，无固定攻防、无跳局轮换

- 测试4：4\-1、4\-2提前结束对局，轮换逻辑不报错、不卡死、不重复计数

**BUG判定标准**：选边失效、单局结束不换边、轮换顺序混乱、提前结束逻辑异常

### 阶段3：复活机制调试（玩家核心属性）

**调试目标**：验证每局复活次数、复活时长、淘汰规则、跨局重置正常

**调试指令/操作**：手动死亡测试、跨小局核对复活数据

- 测试1：每小局开局，所有玩家复活次数固定为2次，无多无少

- 测试2：首次死亡扣除1次复活次数，剩余1次；二次死亡直接淘汰为旁观者

- 测试3：每次复活严格5秒倒计时，无加速、无延迟、无卡死

- 测试4：新小局开启后，所有淘汰玩家复活次数强制重置为2次，全员重置无遗漏

**BUG判定标准**：复活次数异常、时长偏差、耗尽不淘汰、跨局不重置、淘汰后可复活

### 阶段4：资源系统调试（经济平衡核心）

**调试目标**：验证资源刷新规则、钻石掉落、提速效果正常

**调试指令/操作**：挂机观察基地生成、击杀敌方玩家测试掉落

- 测试1：基地生成器全程仅刷新铁、金块，无任何钻石刷新，无遗漏点位

- 测试2：击杀敌方玩家稳定掉落钻石，掉落正常无丢失、无多出

- 测试3：铁金刷新速度明显快于Hypixel原版30%，节奏提速生效

**BUG判定标准**：基地刷钻石、击杀无钻石掉落、资源速度与原版一致/变慢

### 阶段5：装备\&升级保存调试（机制差异化核心）

**调试目标**：验证装备重置、团队升级永久保存规则精准生效

**调试指令/操作**：购买装备、死亡测试、跨小局核对升级buff

- 测试1：铁、金购买的盔甲、武器、道具，玩家死亡后全部清空销毁

- 测试2：钻石装备、道具死亡同步清空，无残留物品

- 测试3：购买团队升级buff后，跨多小局、玩家死亡、对局轮换均不丢失

- 测试4：每小局结束，全员背包、盔甲自动清空，无遗留道具、无叠加物品

**BUG判定标准**：装备死亡保留、团队升级丢失、跨局残留物品

### 阶段6：BO6计分\&对局结束逻辑调试（赛事闭环）

**调试目标**：验证计分统计、夺冠判定、加时触发逻辑正常

**调试指令/操作**：手动输赢小局，累计比分测试结束条件

- 测试1：单局胜利后队伍计分\+1，计分板实时精准更新，无计分错误

- 测试2：任意队伍累计4分，立刻终止整场赛事，弹出夺冠结算界面

- 测试3：6局打完比分3\-3，正常触发加时赛机制，无卡死、无错误结算

**BUG判定标准**：计分错误、4分不结束、3\-3无加时、提前结算异常

### 阶段7：异常边界DEBUG调试（防崩溃、防漏洞）

- 测试1：对局中途玩家退出，仅变为旁观者，对局不终止，剩余玩家可正常对局

- 测试2：单队全员退出，直接判对面整场胜利，结算正常无报错

- 测试3：倒计时期间禁止新玩家加入，满编房间自动锁人

- 测试4：跨小局无数据残留，无复活次数叠加、无装备残留、无计分错乱

## 五、全局固定配置参数表（可直接写入插件Config）

完整yml配置，全覆盖对局、节奏、机制、调试参数，支持直接部署使用

```Plain Text
# BedWars BO6 极速攻防 完整Config配置
# ====================== 数据库存储切换配置（核心可自选） ======================
database:
  # 存储模式可选：LOCAL(本地文件) / SQLITE / MYSQL / REDIS_CACHE
  storage_mode: LOCAL
  # 是否开启全局缓存（推荐开启，提升计分板速度）
  enable_global_cache: true
  # 对局结束自动清理过期临时数据
  auto_clear_expire_data: true

  # 本地文件存储配置（storage_mode=LOCAL 生效）
  local:
    data_folder: "plugins/BedWarsBO6/data/"
    auto_save_tick: 200
    auto_repair_file: true

  # SQLite配置（storage_mode=SQLITE 生效）
  sqlite:
    file_path: "plugins/BedWarsBO6/sqlite/bedwars.db"
    sync_write: false
    auto_migrate: true

  # MySQL远程数据库配置（storage_mode=MYSQL 生效）
  mysql:
    host: "127.0.0.1"
    port: 3306
    user: "root"
    password: "123456"
    database: "bedwars_bo6"
    use_ssl: false
    max_pool_size: 10

  # Redis高速缓存配置（任意模式均可开启缓存）
  redis:
    host: "127.0.0.1"
    port: 6379
    password: ""
    database: 0
    expire_second: 1800

# ====================== 基础对局配置 ======================
basic:
  # 对局档位配置（仅支持三档，禁用4v4）
  mode_1v1_players: 2
  mode_2v2_players: 4
  mode_3v3_players: 6
  disable_4v4: true
  # 满编开局倒计时
  lobby_countdown_time: 10
  # 赛制规则
  max_round: 6
  win_score: 4

# 复活系统配置
respawn:
  per_round_times: 2
  respawn_tick: 100 # 5秒=100tick
  reset_per_round: true

# 资源节奏配置（对标Hypixel原版提速30%）
resource:
  iron_gold_speed_rate: 1.3
  base_spawn_diamond: false
  # 各档位钻石击杀掉落数量
  drop_diamond_1v1: 2
  drop_diamond_2v2: 3
  drop_diamond_3v3: 4

# 装备与升级留存配置
data_save:
  # 团队升级全局永久保存
  team_upgrade_global_save: true
  # 铁/金装备死亡清空
  cheap_equip_death_clear: true
  # 钻石装备死亡清空
  rare_equip_death_clear: true
  # 每小局结束清空个人物品
  clear_item_per_round: true

# 选边机制配置
side_choose:
  pvp_time: 60
  random_when_draw: true

# 调试功能配置
debug:
  enable_debug_mode: true
  show_function_log: true
  show_data_save_log: true
  auto_reset_error_data: true
```

## 六、全维度数据存储方案（本地文件\+SQLite\+MySQL\+Redis）

全覆盖四种主流存储方式，区分**局内临时数据**、**全局永久数据**、**缓存数据**，适配不同服务器需求

### 1\. 本地文件存储（最简部署，小型服务器专用）

存储路径：`plugins/BedWarsBO6/data/`

- **临时局内数据**：room\.yml（房间人数、档位、倒计时状态、攻防身份）

- **玩家局内数据**：player\_temp\.yml（单局复活次数、本局击杀、手持物品缓存）

- **全局赛事数据**：team\_upgrade\.yml（团队全局升级buff，跨小局保存）

- **计分数据**：score\.yml（两队胜场计分、单局胜负记录）

### 2\. SQLite存储（中小型服务器，无需单独数据库）

存储路径：`plugins/BedWarsBO6/sqlite/bedwars.db`

核心数据表：

- **bw\_room**：房间ID、对局档位、当前局数、攻防队伍、倒计时状态

- **bw\_player\_temp**：玩家UUID、单局复活次数、本局状态、是否淘汰

- **bw\_team\_upgrade**：队伍ID、所有升级等级、全局生效状态、跨局留存标记

- **bw\_score\_record**：赛事ID、队伍比分、单局胜负、对局结束状态

### 3\. MySQL存储（大型服务器、多服同步专用）

远程数据库存储，支持多服数据互通、赛事记录永久留存，表结构同SQLite，新增同步字段

- 新增字段：server\_id、create\_time、update\_time、is\_finish

- 适用场景：多服联机、排位赛事、数据永久归档

### 4\. Redis存储（高速缓存、实时对局数据）

仅存储高频读写临时数据，降低数据库压力，实时更新计分板/对局状态

- **Key规则**：bw:room:\{房间ID\}:xxx

- 缓存内容：实时倒计时、玩家在线状态、当前攻防身份、实时比分、复活倒计时

- 过期机制：对局结束自动清空缓存，无残留数据

### 5\. 数据库模式自适应分工规则（Config一键切换，全模式兼容）

插件支持**无需修改代码、仅修改配置文件**自由切换存储方案，四种存储模式完全独立适配，自动适配数据读写、缓存、落盘逻辑，适配所有服务器体量，同时配备故障兜底机制，杜绝崩服、数据丢失。

#### 模式1：LOCAL 本地文件模式（默认模式）

- 适用场景：单机私服、小型服务器、无数据库环境，轻量化部署

- 存储载体：YAML本地文件，无需额外环境依赖

- 核心存储数据：房间对局数据、玩家临时数据、团队全局升级数据、赛事计分记录

- 运行机制：内存实时缓存数据，定时自动落盘保存，支持文件自动修复

#### 模式2：SQLITE 本地轻量数据库模式

- 适用场景：中小型服务器，想要数据库结构化存储、无需独立MySQL服务

- 存储载体：本地单文件db数据库，内置无需额外搭建

- 核心优势：相比YAML，支持数据查询、批量迁移、数据校验、异常修复，稳定性更强

- 专属功能：开启自动迁移后，可一键将旧本地YAML数据迁移至SQLite数据库

#### 模式3：MYSQL 远程数据库模式

- 适用场景：大型服务器、多服联动、排位赛事、需要永久数据归档的场景

- 存储载体：远程独立MySQL数据库，支持多服务器数据互通

- 数据拓展：在SQLite表结构基础上新增服务ID、创建时间、更新时间、对局完结状态等同步字段

- 核心特性：数据永久留存、支持后台数据统计、跨服对局数据同步、高并发读写

#### 模式4：REDIS 高速缓存辅助模式（可搭配任意主存储）

- 定位：不替代本地文件/SQLite/MySQL主存储，仅作为**实时数据高速缓存加速**

- 缓存数据：计分板实时数据、大厅倒计时、玩家在线状态、攻防身份、复活次数、实时比分等高频读写数据

- 固化数据：团队升级、对局历史记录、赛事结算数据依旧落盘至主存储

- 清理机制：对局结束自动清空对应Redis缓存，无垃圾数据堆积，无数据残留错乱

### 6\. 全局数据存储分工标准（统一逻辑，全模式通用）

- 临时动态数据（实时变更）：优先Redis缓存，无Redis则走本地内存缓存

- 对局进度永久数据：中小型服优先SQLite，大型多服优先MySQL，极简部署用本地文件

- 团队全局升级数据：高优先级数据库存储，数据库连接失败自动兜底本地文件

- 计分板展示数据：强制Redis实时刷新，保障2tick极致流畅刷新，无卡顿延迟

### 7\. 数据库自动适配\&容错机制（核心开发保障）

- 启动自适应：插件启动自动读取config中`storage_mode`参数，无需改代码，自动初始化对应存储驱动

- 业务解耦：所有数据读写函数统一封装，切换任意存储模式，核心对局逻辑、计分、调试功能完全不受影响

- 故障兜底：MySQL/SQLite连接失败、报错时，自动降级为本地文件存储，防止插件禁用、服务器崩服

- DEBUG日志：调试模式下，实时打印当前存储模式、数据读写耗时、读写状态、异常报错详情，方便逐行排错

- 自动运维：对局结束自动清理过期临时数据、无效缓存、残留脏数据，长期运行不卡顿、不堆积垃圾数据

### 6\. 数据库自动适配逻辑（插件内置无需手动改代码）

- 启动读取 **storage\_mode** 自动初始化对应存储驱动

- 所有读写函数统一封装，**切换数据库无需改动业务代码**

- DEBUG模式下自动打印：当前存储模式、读写耗时、数据校验结果

- 异常兜底：数据库连接失败自动降级为本地文件存储，防止插件崩服

- 临时局内动态数据 → Redis/本地文件

- 赛事永久进度数据 → SQLite/MySQL

- 团队升级全局数据 → 优先数据库存储，兜底本地文件

- 实时计分板数据 → Redis实时刷新

### 7\. 全套数据库DEBUG调试指令（可直接游戏内/控制台执行）

所有指令支持**全存储模式通用**，可一键检测连接、读写、数据一致性、缓存状态，快速排查数据库BUG

|调试指令|指令功能|适用存储模式|调试输出内容|
|---|---|---|---|
|`/bw db test`|一键测试当前数据库连接是否正常|LOCAL/SQLITE/MYSQL/REDIS|当前存储模式、连接状态、连接耗时、是否正常初始化|
|`/bw db readtest`|测试数据读取功能，校验数据完整性|全模式通用|读取团队升级、对局计分、玩家临时数据，输出数据是否缺失、错乱|
|`/bw db writetest`|测试数据写入\&落盘功能|全模式通用|写入测试数据、自动校验落盘结果、测试读写一致性|
|`/bw db cache clear`|手动清空Redis缓存/本地内存缓存|带缓存所有模式|清空对局实时缓存、计分板缓存，输出缓存清理条数|
|`/bw db migrate`|一键数据迁移（YAML → SQLite/MySQL）|SQLITE/MYSQL|迁移进度、迁移成功数据条数、失败报错详情|
|`/bw db resetroom`|强制重置所有房间临时数据|全模式通用|清空残留对局数据、重置倒计时、攻防状态、计分缓存|
|`/bw db checkdata`|全局数据脏数据校验|全模式通用|检测复活次数残留、计分错乱、团队升级异常、缓存不匹配问题|
|`/bw db fallback`|手动触发数据库降级兜底|SQLITE/MYSQL|强制切换为本地文件存储，备份原数据库数据|

### 8\. 数据库专属BUG判定标准

- 连接异常：config配置正确但提示连接失败、插件启动无存储初始化日志

- 读写异常：数据修改后不落地、重启服务器数据丢失、跨局数据错乱

- 缓存异常：Redis开启后计分板卡顿、数据更新延迟、新旧数据叠加

- 迁移异常：YAML迁移数据库后数据缺失、字段不匹配、对局报错

- 兜底异常：数据库崩溃后未自动降级本地文件，直接报错禁用插件

- 启动读取 **storage\_mode** 自动初始化对应存储驱动

- 所有读写函数统一封装，**切换数据库无需改动业务代码**

- DEBUG模式下自动打印：当前存储模式、读写耗时、数据校验结果

- 异常兜底：数据库连接失败自动降级为本地文件存储，防止插件崩服

- 临时局内动态数据 → Redis/本地文件

- 赛事永久进度数据 → SQLite/MySQL

- 团队升级全局数据 → 优先数据库存储，兜底本地文件

- 实时计分板数据 → Redis实时刷新

## 七、完整计分板功能设计（全局实时刷新）

### 1\. 计分板展示场景（分阶段切换界面）

#### 场景一：大厅等待阶段

标题：§6BedWars 极速BO6攻防

内容：

§f模式档位：\{1v1/2v2/3v3\}

§f当前人数：\{现有人数\}/\{满编人数\}

§f状态：等待补齐玩家

§7——————————

§f规则：满人自动开局

#### 场景二：满编倒计时阶段

标题：§6BedWars 极速BO6攻防

内容：

§f对局模式：\{档位\}

§a对局即将开启：\{剩余秒数\}s

§7——————————

§f满编就绪 · 即将选边

#### 场景三：单局对战阶段（核心｜全数据展示，无重复冗余）

标题：§6BO6 极速攻防对战

内容：

§b当前BO对局：\{当前局数\}/6

§f队伍比分：红队 \{红分\} \- \{蓝分\} 蓝队

§f当前进攻方：\{进攻队伍名称\}

§f当前防守方：\{防守队伍名称\}

§7——————————

§e个人本局击杀：\{击杀数\}

§c剩余复活次数：\{剩余次数\}/2

§a团队全局升级：已生效

标题：§6BO6 极速攻防对战

内容：

§b当前对局：BO\{当前总局数\}/6

§f队伍比分：红\{红分\} \- 蓝\{蓝分\}

§f进攻方：\{当前进攻队伍\}

§f防守方：\{当前防守队伍\}

§7——————————

§e个人本局击杀：\{玩家本局击杀数\}

§c剩余复活次数：\{剩余次数\}/2

§a团队升级：已生效

#### 场景四：对局结算阶段

展示最终比分、获胜队伍、总攻防次数，停留5秒自动关闭

### 2\. 计分板刷新\&数据绑定规则

- 基础刷新频率：2tick实时刷新，界面流畅无卡顿

- **核心数据强制绑定刷新**：当前BO局数、队伍比分、攻防身份、个人击杀数、剩余复活次数

- 增量刷新机制：无数据变动时静默刷新，仅数值变更时重绘界面，极低性能消耗

- 数据兜底展示：无击杀时显示0、复活次数耗尽显示0、未开局不展示对局数据，无空白错乱

- 基础刷新频率：2tick实时刷新（流畅不卡顿）

- 数据变更强制刷新：人数变动、比分变动、攻防切换、复活次数变动

- 无数据变动静默刷新，降低性能消耗

## 八、独立函数调用总表（插件开发专用，可直接对接）

所有核心功能独立封装函数，解耦开发，支持单独调试、单独调用、DEBUG检测

|函数名称|功能作用|调用时机|返回值|调试可用|
|---|---|---|---|---|
|checkFullPlayer\(\)|检测房间是否满编，匹配对应档位人数|玩家加入/退出房间时|Boolean|是|
|startLobbyCountdown\(\)|开启满编开局倒计时，刷新计分板|房间满编检测通过时|Void|是|
|cancelCountdownTask\(\)|终止并销毁倒计时任务，重置大厅状态|倒计时中玩家退出时|Void|是|
|chooseFirstSide\(\)|执行选边逻辑（拼刀/随机），确定首局攻防|倒计时结束开局时|Team\(进攻队,防守队\)|是|
|swapAttackDefend\(\)|单局结束强制互换两队攻防身份|每小局结算完毕后|Void|是|
|resetPlayerRespawnTime\(\)|重置全员单局复活次数为2次|每新小局开局时|Void|是|
|playerDeathClearItem\(\)|玩家死亡清空铁/金/钻石装备道具|玩家死亡瞬间|Void|是|
|saveTeamUpgradeData\(\)|持久化保存团队全局升级数据|升级购买后、对局轮换时|Boolean|是|
|dropKillDiamond\(\)|击杀玩家掉落对应档位钻石数量|玩家击杀敌方时|Void|是|
|updateScoreBoard\(\)|实时更新全局计分板所有数据|数据变动/定时刷新|Void|是|
|checkGameWin\(\)|检测队伍是否达到4胜夺冠条件|每小局结算后|Boolean/Team|是|
|roundEndReset\(\)|单局结束清空玩家物品、结算比分|单局对战结束时|Void|是|
|debugDataCheck\(\)|全局数据校验，排查残留、错乱数据|DEBUG模式下每局开局前|String\(报错日志\)|是|

## 九、最终完整对局闭环流程

选择对局档位 → 等待玩家满编 → 自动触发10s倒计时开局 → 选边确定首局攻防 → 开启单局对战（2次5s复活\+极速资源\+击杀掉钻石） → 单局结束结算计分、清空个人装备、强制互换攻防 → 循环对局 → 任意队伍4分夺冠/3\-3触发加时 → 赛事结算结束

> （注：部分内容可能由 AI 生成）
